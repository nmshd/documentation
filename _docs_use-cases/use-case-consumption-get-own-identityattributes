---
---

{{properties.description}}

{% include properties_list.html %}

This use-case is intended to return all [IdentityAttributes]({% link _docs_integrate/data-model-overview.md %}#identityattribute) you are the owner of and whose `shareInfo` is undefined.
In case of an Attribute succession, by default only the latest version will be returned.

## Parameters

-   Optionally, `onlyLatestVersions` can be disabled, such that in case of an Attribute succession all versions will be returned.

## On Success

-   An array of [LocalAttributes]({% link _docs_integrate/data-model-overview.md %}#localattribute) will be returned. It contains all your unshared IdentityAttributes.

## On Failure

-   No Attributes can be returned, if the parameter is malformed.
